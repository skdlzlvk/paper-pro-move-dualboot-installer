# Early boot selector

`rm-boot-selector` is the project-authored, E Ink-oriented stock/Android boot
choice UI. It uses the same MIT-licensed Oxide interface declaration as the
native display bridge and resolves the device-owned E Ink library at runtime.

The selector does not contain a stock firmware image or reMarkable application.
Build it with `../build-native.sh` and the Paper Pro Move SDK.
